const { Schema, model } = require('mongoose');

const newSchedule = new Schema({
  dateService: { type: Date, required:true },
  hour: 
  idsalao
  idusuario
  idservico:
  dateOfService: 
},
{
  timestamps: true,
})